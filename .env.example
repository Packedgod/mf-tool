# Security-level fundamentals (P/E, P/B, ROE, margins, leverage) for portfolio holdings.
# Drives the "Current holdings quality" pillar and the valuation, earnings-momentum and
# portfolio-quality factors inside Current Opportunity.
#
# Optional. Without a key the app resolves these from BSE's public quote endpoint, which
# covers valuation and profitability but not gearing or liquidity ratios; Yahoo Finance is
# tried last for those and is bot-throttled for server-side callers, so the factors that
# depend on them report reduced coverage rather than guessing.
# Free tier: https://site.financialmodelingprep.com/
FMP_API_KEY=
