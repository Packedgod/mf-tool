node_modules
.next
.vercel
.env
.env.local
*.log
.DS_Store

# Locally accumulated point-in-time journals (membership ledger, EPS history,
# month-end portfolio archive). Machine-local and append-only; not source.
data/journal/
